import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import Input from './Input';

<Meta title="Form/Input" />

# Input

[Source code](https://github.com/chewhx/lyteui/blob/main/src/components/Input/Input.tsx)

```jsx
import { Input } from 'lyteui';
```

## Overview

Base input field component.

**To create forms, use other specific input components, and not this directly.**

<Canvas>
	<Input placeholder="Default input field" />
</Canvas>

## API

<ArgsTable of={Input} />

## Default

<Canvas>
	<Input placeholder="Default" />
</Canvas>

## Radius

<Canvas>
	<Input placeholder="radius xs" radius="xs" />
	<Input placeholder="radius sm" radius="sm" />
	<Input placeholder="radius md" radius="md" />
	<Input placeholder="radius lg" radius="lg" />
	<Input placeholder="radius xl" radius="xl" />
</Canvas>

## Sizes

<Canvas>
	<Input placeholder="size sm" size="sm" />
	<Input placeholder="size md" size="md" />
	<Input placeholder="size lg" size="lg" />
</Canvas>

## Types

Text, number, date, datetime, file, color, password

<Canvas>
	<Input type="text" placeholder="text" />
	<Input type="number" placeholder="number" />
	<Input type="date" placeholder="date" />
	<Input type="time" placeholder="time" />
	<Input type="file" placeholder="file" />
	<Input type="color" />
	<Input type="password" placeholder="password" />
</Canvas>

## Full width

<Canvas>
	<div style={{ width: '500px', margin: 'auto' }}>
		<Input placeholder="sm" fullWidth size="sm" />
		<Input placeholder="md" fullWidth size="md" />
		<Input placeholder="lg" fullWidth size="lg" />
	</div>
</Canvas>

## Validation

Display a validation state to indicate feedback to user's input

<Canvas>
	<Input placeholder="valid" isValid />
	<Input placeholder="invalid" isInvalid />
</Canvas>
